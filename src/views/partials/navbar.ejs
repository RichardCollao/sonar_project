<nav class="navbar navbar-expand-lg bg-body-tertiary border-bottom sticky-top">
  <div class="container">
    <a class="navbar-brand fw-semibold" href="/sonar">
      <i class="bi bi-tools me-2"></i>DevUtils
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
      aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= activePage === 'sonar' ? 'active' : '' %>" aria-current="<%= activePage === 'sonar' ? 'page' : 'false' %>" href="/sonar">
            <i class="bi bi-cloud-check me-1"></i>Sonar
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activePage === 'gitleaks' ? 'active' : '' %>" aria-current="<%= activePage === 'gitleaks' ? 'page' : 'false' %>" href="/gitleaks">
            <i class="bi bi-shield-lock me-1"></i>Gitleaks
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= (activePage === 'semgrep' || activePage === 'semgrep_config') ? 'active' : '' %>" aria-current="<%= (activePage === 'semgrep' || activePage === 'semgrep_config') ? 'page' : 'false' %>" href="/semgrep">
            <i class="bi bi-search me-1"></i>Semgrep
          </a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link <%= activePage === 'tools_info' ? 'active' : '' %>" aria-current="<%= activePage === 'tools_info' ? 'page' : 'false' %>" href="/tools_info">
            <i class="bi bi-info-circle me-1"></i>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= activePage === 'app_config' ? 'active' : '' %>" aria-current="<%= activePage === 'app_config' ? 'page' : 'false' %>" href="/app_config">
            <i class="bi bi-gear me-1"></i>          </a>
        </li>
      </ul>
    </div>
  </div>
</nav>
